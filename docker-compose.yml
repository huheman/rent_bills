version: "3.9"

services:
  serverless-rent-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: serverless-rent-backend
    env_file:
      - .env
    ports:
      - "8000:8000"
    restart: unless-stopped
